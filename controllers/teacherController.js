const debug = require('debug')('teacher-daily-record:teachers');
const Joi = require('joi');

exports.createTeacherDailyRecord = (req, res) => {
  // Validate and create teacher daily record
};

exports.getTeacherDailyRecords = (req, res) => {
  
  // Fetch and return teacher daily records
};
