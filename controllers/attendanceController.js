const debug = require('debug')('teacher-daily-record:attendance');
const Joi = require('joi');

exports.recordAttendance = (req, res) => {
  // Validate and record student attendance
};

exports.getAttendanceReport = (req, res) => {
  // Fetch and return attendance report
};
