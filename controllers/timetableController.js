const debug = require('debug')('teacher-daily-record:timetable');
const Joi = require('joi');

exports.createTimetable = (req, res) => {
  // Validate and create timetable
};

exports.getTeacherTimetable = (req, res) => {
  // Fetch and return teacher timetable
};
